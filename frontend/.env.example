# Copy this to .env and fill in your values.
# For local dev, the default (no file needed) points to localhost.

# Production: set this to your server's API root, e.g.:
# VITE_API_BASE=https://yourdomain.com/LAS/backend/api
VITE_API_BASE=http://localhost/LAS/backend/api
